/**
 * A sample file that represents the API of an object to mock.
 *
 * For exmaple, there could be some code in a file that calls on functionality
 * on this API.
 */

#include "bar.h"

// A C++ Style comment

/* A single line comment */
extern void foo_abc(int x, char * y);

/*
 * A multiline comment with a blank line

*/
extern int foo_xyz(void);

extern int x; // an x thing with inline comment.

/***** something cool to define (*******/
#define X

/* another thing */
#undef     Y
